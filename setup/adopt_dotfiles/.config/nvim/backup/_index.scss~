@use "@styles/settings/index.scss" as *;

.legend {
  /* background-color: $color-green-charleston; */
}

/* .legend__figure { */
/*   overflow: hidden; */
/* } */

.legend__image {
  position: absolute;
  top: 100px;
  left: 100px;

  width: 800px;
  height: 500px;
  object-fit: contain;
}

.legend__caption {
  display: flex;
  flex-flow: column nowrap;
  gap: 5px;

  position: absolute;

  bottom: 100px;
  right: 100px;

  background-color: $color-white-bone;
}

.legend__name {
  font-weight: 500;
}

.legend__nickname {
  font-weight: 400;

  opacity: 0.7;
}

.legend__interests {
  padding: 5px 0;

  line-height: 1.2;
  font-weight: 500;
}

.legend__nav {
  padding: 30px 0;
  text-align: center;
}

.contentNavList {
  display: flex;
  flex-flow: column nowrap;
  gap: 20px;

  text-align: space-around;

  font-size: 1.2rem;
  font-weight: bold;
}

.contentNavList__item {
}

.contentNavList__item--active {
  @extend .contentNavList__item;

  a {
    position: relative;
  }
}
