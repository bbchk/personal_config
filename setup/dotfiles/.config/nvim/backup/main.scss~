@use "settings/index" as *;
@use "elements/index" as *;

@use "settings/reset.scss" as *;
@use "settings/normalize.scss" as *;

@use "comps/layout/asideNavbar" as *;

@use "pages/welcome/index" as *;

html {
  overscroll-behavior: none;

  scroll-behavior: smooth;

  font-family: "Ubuntu";

  body {
    scroll-behavior: smooth;
    overflow-x: hidden;

    font-weight: 300;

    background-color: $color-background;

    a {
      cursor: pointer;
    }
  }
}
