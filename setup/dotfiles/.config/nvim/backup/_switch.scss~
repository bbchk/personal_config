@use "settings/index" as *;
@use "settings/mixins" as *;

.switch {
  border: solid 1px $color-primary;
  border-radius: 20px;
  overflow: hidden;

  height: min-content;

  background-color: $color-black-rich;
}

.switch__select {
  display: none;

  border-radius: 10px;
  overflow: hidden;

  padding: 4px;

  background-color: $color-primary;

  color: $color-black;

  font-weight: bold;
  font-size: 1.2rem;
  font-family: $font-serif;
}

.switch__menu {
  display: flex;
  gap: 30px;
  justify-content: space-between;
  align-items: center;

  padding: 4px 8px;
}

.switch__item {
  button {
    padding: 2px 16px;
    border-radius: 40px;

    white-space: nowrap;
    font-weight: bold;
    font-family: $font-serif;

    color: $color-white;

    transition:
      background 0.25s ease-in-out,
      color 0.25s ease-in-out;

    &:hover {
      background: $color-primary;
      color: $color-black;
    }
  }
}

.switch__item--active {
  @extend .switch__item;

  button {
    color: $color-black;
    background-color: $color-primary;
  }
}
