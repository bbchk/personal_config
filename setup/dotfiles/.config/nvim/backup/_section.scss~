@use "@styles/settings/index" as *;

.contentSection {
  display: flex;
  flex-flow: column nowrap;
  gap: 20px;

  padding: 20px;

  h2 {
    font-size: 1.2rem;
    border-bottom: 2px solid $color-white-lightsilv;
  }
}
