return {
	"nvim-treesitter/nvim-treesitter-textobjects",
	-- Pin to `master` to match nvim-treesitter (see editor__treesitter.lua). The
	-- textobjects module is configured through the classic configs.setup{} there.
	branch = "master",
}
